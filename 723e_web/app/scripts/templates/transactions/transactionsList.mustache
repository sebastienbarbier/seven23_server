<h2>Liste des Débits/Crédits</h2>

<ul>
	{{# debitscredits }}
	<li class="debitcredit" data-id="{{ id }}">{{ name }} {{ date }} {{ amount }} {{ value }} {{ currency }}
		<button type="button" class="btn btn-primary btn-xs edit">Modifier</button>
		<button type="button" class="btn btn-primary btn-xs delete">Supprimer</button>
	</li>
	{{/ debitscredits }}
	{{^ debitscredits}}
	 <p>Aucun debitscredtis n'est actuellement renseigné</p>
	{{/ debitscredits}}
</ul>
